'use client'

import Main from "./components/Main";

const Home = () => {
return (
	<div style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
		<Main/>
	</div>
);
};

export default Home;
